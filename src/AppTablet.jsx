// AppTablet.jsx — POC stub
// Ce composant sera développé dans une prochaine phase
// Pour l'instant, il redirige vers AppDesktop

import React from "react";
import AppDesktop from "./AppDesktop";

export default function AppTablet() {
  // TODO Phase 2 : Créer un layout optimisé tablette
  // Pour l'instant, on utilise le layout Desktop en attendant
  return <AppDesktop />;
}
