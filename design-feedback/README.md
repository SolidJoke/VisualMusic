# `design/feedback` — design exchange with Claude Design

This branch is **never merged**. It carries, as text, what a design session needs and cannot see from the code alone.
Screenshots are **not** stored here (a public repo's clones would carry them forever): they are attached directly to the
Claude Design conversation, and later mockups go through the Canva folder **"VisualMusic — Claude Design"**.

| Path | What |
|---|---|
| `decisions.md` | Decisions in force: product frame, validated canvas, adopted direction and its arbitration, build order. **Read first.** |
| `canevas/artboards/*.dc.html` | Canvas VMU-082, validated by Gabriel on 2026-09-21, one file per screen (`Main`, `V1Types`, `V1Phone`, `V3Arrival`, `V3Playing`, `V3Phone`, `V3PhoneComposer`, `ChordCell`; `V2*` were the rejected alternative, kept for comparison). |
| `canevas/theme-values.md` | Where each theme value of the canvas comes from (read on the production build). |
| `claude-design-feedback-2026-09-23.md` | Claude Design's previous feedback, read from the code at `d277303`. |
| `reports/` | Claude Design session reports, once Gabriel relays them and the coordinator files them. |

Code reference for this exchange: `main` at `f6ac1b0` (2026-09-23).
